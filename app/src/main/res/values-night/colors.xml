<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="primaryColor">#3949ab</color>
    <color name="primaryLightColor">#6f74dd</color>
    <color name="primaryDarkColor">#00227b</color>
    <color name="primaryTextColor">#ffffff</color>

    <color name="actionBarBackgroundColor">@color/primaryColor</color>
    <color name="bottomNavViewColor">@color/primaryColor</color>
    <color name="bottomNavViewTextColor">@color/primaryTextColor</color>

    <!-- RecyclerView -->
    <color name="pageBackground">@color/black</color>
    <color name="rvTextColor">@color/primaryTextColor</color>
    <color name="rvItemTitleColor">@color/primaryTextColor</color>
    <color name="placeholderColor">@color/primaryLightColor</color>

    <!-- Custom BottomNav Color -->
    <color name="checked">@color/primaryTextColor</color>
    <color name="unchecked">@color/primaryLightColor</color>
</resources>